-- generation_jobsは常駐バックグラウンドループ方式のジョブ管理用だったが、
-- Vercelサーバーレスでは常駐ループが継続する保証が無く、再クレームで
-- 試行上限がリセットされ得る（コスト暴走の危険）ため方式を変更。
-- 新方式はリクエスト単位で最大1回だけ生成を試みる「オンデマンド」方式とし、
-- 上限はquestionsテーブルの行数を都度数えるだけで判定する（リセットされ得る
-- 状態を一切持たない）。よってジョブ管理テーブルは不要になった。
drop table if exists generation_jobs;
